# ========================================
# YTDLP FastAPI 环境变量配置示例
# ========================================

# ===== 核心模式配置 =====
# 1=直链模式 (快速,适合开发测试)
# 0=代理模式 (支持高并发,适合生产环境)
DIRECT_LINK_MODE=0

# ===== WARP 代理池配置 =====
ENABLE_WARP_PROXY=1
WARP_CONFIG_DIR=warp-configs
PROXY_HEALTH_CHECK_INTERVAL=300

# ===== 域名和访问控制 =====
# 允许访问的域名白名单，支持通配符，多个域名用逗号分隔
ALLOWED_HOSTS=localhost,127.0.0.1,*.vercel.app

# ===== API 鉴权配置 =====
# 主 API 访问密钥（客户端需要在 X-Secret 请求头中传递此值）
SECRET_KEY=your-main-secret-key-2024

# 多域名多密钥配置（高级模式，可选）
# 格式：domain1:key1,domain2:key2
# 示例：site1.com:key123,site2.com:key456,*.vercel.app:vercelkey
MULTI_DOMAIN_KEYS=

# ===== 加密配置(代理模式需要) =====
# 加密密钥（仅代理模式需要，直链模式忽略）
CRYPT_KEY=fl5JcIwHh0SM87Vl18B_Sn65lV0whYIQ3fnfGYqpVlE=

# 加密数据有效期（仅代理模式需要，直链模式忽略）
CRYPT_TTL=28800

# ===== Railway 部署配置 =====
# 应用监听端口（Railway会自动设置，本地开发可指定）
PORT=8000

# ===== 功能开关 =====
# 禁用人机验证（简化，不需要验证码）
DISABLE_TURNSTILE=1

# 禁用 API 文档（可选，0=显示文档，1=隐藏文档）
DISABLE_DOCS=0

# 禁用演示页面（可选，0=显示演示，1=隐藏演示）
DISABLE_DEMO=0

# 禁用额外的主机验证（简化）
DISABLE_HOST_VALIDATION=1
